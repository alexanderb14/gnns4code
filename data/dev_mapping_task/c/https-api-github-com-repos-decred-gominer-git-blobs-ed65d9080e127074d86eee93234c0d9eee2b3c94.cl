__constant uint Ga = 0x243F6A88UL; __constant uint Gb = 0x85A308D3UL; __constant uint Gc = 0x13198A2EUL; __constant uint Gd = 0x03707344UL; __constant uint Ge = 0xA4093822UL; __constant uint Gf = 0x299F31D0UL; __constant uint Gg = 0x082EFA98UL; __constant uint Gh = 0xEC4E6C89UL; __constant uint Gi = 0x452821E6UL; __constant uint Gj = 0x38D01377UL; __constant uint Gk = 0xBE5466CFUL; __constant uint Gl = 0x34E90C6CUL; __constant uint Gm = 0xC0AC29B7UL; __constant uint Gn = 0xC97C50DDUL; __constant uint Go = 0x3F84D5B5UL; __constant uint Gp = 0xB5470917UL; __attribute__((reqd_work_group_size(128, 1, 1))) __kernel void A(volatile __global uint* restrict a, const uint b, const uint c, const uint d, const uint e, const uint f, const uint g, const uint h, const uint i, const uint j, const uint k, const uint l, const uint m, const uint n, const uint o, const uint p, const uint q, const uint r, const uint s, const uint t, const uint u ) {
  uint v, w, x, y, z, aa, ab, ac;
  uint ad, ae, af, ag, ah, ai, aj, ak;
  uint al, am, an, ao, ap, aq, ar, as;
  uint at, au, av, aw, ax, ay, az, ba;
  uint bb;

  al = b;
  am = c;
  an = d;
  ao = e;
  ap = f;
  aq = g;
  ar = h;
  bb = as = i;
  at = Ga;
  au = Gb;
  av = Gc;
  aw = Gd;
  ax = 0xA4093D82UL;
  ay = 0x299F3470UL;
  az = Gg;
  ba = Gh;

  uint bc = get_global_id(0);

  v = j;
  w = k;
  x = l;
  y = bc;
  z = m;
  aa = n;
  ab = o;
  ac = p;
  ad = q;
  ae = r;
  af = s;
  ag = t;
  ah = u;
  ai = 0x80000001UL;
  aj = 0x00000000UL;
  ak = 0x000005a0UL;

  al = al + (v ^ Gb);
  al = al + ap;
  ax = ax ^ al;
  ax = rotate((uint)(ax), (uint)(32 - (16)));
  at = at + ax;
  ap = ap ^ at;
  ap = rotate((uint)(ap), (uint)(32 - (12)));
  am = am + (x ^ Gd);
  am = am + aq;
  ay = ay ^ am;
  ay = rotate((uint)(ay), (uint)(32 - (16)));
  au = au + ay;
  aq = aq ^ au;
  aq = rotate((uint)(aq), (uint)(32 - (12)));
  an = an + (z ^ Gf);
  an = an + ar;
  az = az ^ an;
  az = rotate((uint)(az), (uint)(32 - (16)));
  av = av + az;
  ar = ar ^ av;
  ar = rotate((uint)(ar), (uint)(32 - (12)));
  ao = ao + (ab ^ Gh);
  ao = ao + as;
  ba = ba ^ ao;
  ba = rotate((uint)(ba), (uint)(32 - (16)));
  aw = aw + ba;
  as = as ^ aw;
  as = rotate((uint)(as), (uint)(32 - (12)));
  an = an + (aa ^ Ge);
  an = an + ar;
  az = az ^ an;
  az = rotate((uint)(az), (uint)(32 - (8)));
  av = av + az;
  ar = ar ^ av;
  ar = rotate((uint)(ar), (uint)(32 - (7)));
  ao = ao + (ac ^ Gg);
  ao = ao + as;
  ba = ba ^ ao;
  ba = rotate((uint)(ba), (uint)(32 - (8)));
  aw = aw + ba;
  as = as ^ aw;
  as = rotate((uint)(as), (uint)(32 - (7)));
  am = am + (y ^ Gc);
  am = am + aq;
  ay = ay ^ am;
  ay = rotate((uint)(ay), (uint)(32 - (8)));
  au = au + ay;
  aq = aq ^ au;
  aq = rotate((uint)(aq), (uint)(32 - (7)));
  al = al + (w ^ Ga);
  al = al + ap;
  ax = ax ^ al;
  ax = rotate((uint)(ax), (uint)(32 - (8)));
  at = at + ax;
  ap = ap ^ at;
  ap = rotate((uint)(ap), (uint)(32 - (7)));
  al = al + (ad ^ Gj);
  al = al + aq;
  ba = ba ^ al;
  ba = rotate((uint)(ba), (uint)(32 - (16)));
  av = av + ba;
  aq = aq ^ av;
  aq = rotate((uint)(aq), (uint)(32 - (12)));
  am = am + (af ^ Gl);
  am = am + ar;
  ax = ax ^ am;
  ax = rotate((uint)(ax), (uint)(32 - (16)));
  aw = aw + ax;
  ar = ar ^ aw;
  ar = rotate((uint)(ar), (uint)(32 - (12)));
  an = an + (ah ^ Gn);
  an = an + as;
  ay = ay ^ an;
  ay = rotate((uint)(ay), (uint)(32 - (16)));
  at = at + ay;
  as = as ^ at;
  as = rotate((uint)(as), (uint)(32 - (12)));
  ao = ao + (aj ^ Gp);
  ao = ao + ap;
  az = az ^ ao;
  az = rotate((uint)(az), (uint)(32 - (16)));
  au = au + az;
  ap = ap ^ au;
  ap = rotate((uint)(ap), (uint)(32 - (12)));
  an = an + (ai ^ Gm);
  an = an + as;
  ay = ay ^ an;
  ay = rotate((uint)(ay), (uint)(32 - (8)));
  at = at + ay;
  as = as ^ at;
  as = rotate((uint)(as), (uint)(32 - (7)));
  ao = ao + (ak ^ Go);
  ao = ao + ap;
  az = az ^ ao;
  az = rotate((uint)(az), (uint)(32 - (8)));
  au = au + az;
  ap = ap ^ au;
  ap = rotate((uint)(ap), (uint)(32 - (7)));
  am = am + (ag ^ Gk);
  am = am + ar;
  ax = ax ^ am;
  ax = rotate((uint)(ax), (uint)(32 - (8)));
  aw = aw + ax;
  ar = ar ^ aw;
  ar = rotate((uint)(ar), (uint)(32 - (7)));
  al = al + (ae ^ Gi);
  al = al + aq;
  ba = ba ^ al;
  ba = rotate((uint)(ba), (uint)(32 - (8)));
  av = av + ba;
  aq = aq ^ av;
  aq = rotate((uint)(aq), (uint)(32 - (7)));

  al = al + (aj ^ Gk);
  al = al + ap;
  ax = ax ^ al;
  ax = rotate((uint)(ax), (uint)(32 - (16)));
  at = at + ax;
  ap = ap ^ at;
  ap = rotate((uint)(ap), (uint)(32 - (12)));
  am = am + (z ^ Gi);
  am = am + aq;
  ay = ay ^ am;
  ay = rotate((uint)(ay), (uint)(32 - (16)));
  au = au + ay;
  aq = aq ^ au;
  aq = rotate((uint)(aq), (uint)(32 - (12)));
  an = an + (ae ^ Gp);
  an = an + ar;
  az = az ^ an;
  az = rotate((uint)(az), (uint)(32 - (16)));
  av = av + az;
  ar = ar ^ av;
  ar = rotate((uint)(ar), (uint)(32 - (12)));
  ao = ao + (ai ^ Gg);
  ao = ao + as;
  ba = ba ^ ao;
  ba = rotate((uint)(ba), (uint)(32 - (16)));
  aw = aw + ba;
  as = as ^ aw;
  as = rotate((uint)(as), (uint)(32 - (12)));
  an = an + (ak ^ Gj);
  an = an + ar;
  az = az ^ an;
  az = rotate((uint)(az), (uint)(32 - (8)));
  av = av + az;
  ar = ar ^ av;
  ar = rotate((uint)(ar), (uint)(32 - (7)));
  ao = ao + (ab ^ Gn);
  ao = ao + as;
  ba = ba ^ ao;
  ba = rotate((uint)(ba), (uint)(32 - (8)));
  aw = aw + ba;
  as = as ^ aw;
  as = rotate((uint)(as), (uint)(32 - (7)));
  am = am + (ad ^ Ge);
  am = am + aq;
  ay = ay ^ am;
  ay = rotate((uint)(ay), (uint)(32 - (8)));
  au = au + ay;
  aq = aq ^ au;
  aq = rotate((uint)(aq), (uint)(32 - (7)));
  al = al + (af ^ Go);
  al = al + ap;
  ax = ax ^ al;
  ax = rotate((uint)(ax), (uint)(32 - (8)));
  at = at + ax;
  ap = ap ^ at;
  ap = rotate((uint)(ap), (uint)(32 - (7)));
  al = al + (w ^ Gm);
  al = al + aq;
  ba = ba ^ al;
  ba = rotate((uint)(ba), (uint)(32 - (16)));
  av = av + ba;
  aq = aq ^ av;
  aq = rotate((uint)(aq), (uint)(32 - (12)));
  am = am + (v ^ Gc);
  am = am + ar;
  ax = ax ^ am;
  ax = rotate((uint)(ax), (uint)(32 - (16)));
  aw = aw + ax;
  ar = ar ^ aw;
  ar = rotate((uint)(ar), (uint)(32 - (12)));
  an = an + (ag ^ Gh);
  an = an + as;
  ay = ay ^ an;
  ay = rotate((uint)(ay), (uint)(32 - (16)));
  at = at + ay;
  as = as ^ at;
  as = rotate((uint)(as), (uint)(32 - (12)));
  ao = ao + (aa ^ Gd);
  ao = ao + ap;
  az = az ^ ao;
  az = rotate((uint)(az), (uint)(32 - (16)));
  au = au + az;
  ap = ap ^ au;
  ap = rotate((uint)(ap), (uint)(32 - (12)));
  an = an + (ac ^ Gl);
  an = an + as;
  ay = ay ^ an;
  ay = rotate((uint)(ay), (uint)(32 - (8)));
  at = at + ay;
  as = as ^ at;
  as = rotate((uint)(as), (uint)(32 - (7)));
  ao = ao + (y ^ Gf);
  ao = ao + ap;
  az = az ^ ao;
  az = rotate((uint)(az), (uint)(32 - (8)));
  au = au + az;
  ap = ap ^ au;
  ap = rotate((uint)(ap), (uint)(32 - (7)));
  am = am + (x ^ Ga);
  am = am + ar;
  ax = ax ^ am;
  ax = rotate((uint)(ax), (uint)(32 - (8)));
  aw = aw + ax;
  ar = ar ^ aw;
  ar = rotate((uint)(ar), (uint)(32 - (7)));
  al = al + (ah ^ Gb);
  al = al + aq;
  ba = ba ^ al;
  ba = rotate((uint)(ba), (uint)(32 - (8)));
  av = av + ba;
  aq = aq ^ av;
  aq = rotate((uint)(aq), (uint)(32 - (7)));

  al = al + (ag ^ Gi);
  al = al + ap;
  ax = ax ^ al;
  ax = rotate((uint)(ax), (uint)(32 - (16)));
  at = at + ax;
  ap = ap ^ at;
  ap = rotate((uint)(ap), (uint)(32 - (12)));
  am = am + (ah ^ Ga);
  am = am + aq;
  ay = ay ^ am;
  ay = rotate((uint)(ay), (uint)(32 - (16)));
  au = au + ay;
  aq = aq ^ au;
  aq = rotate((uint)(aq), (uint)(32 - (12)));
  an = an + (aa ^ Gc);
  an = an + ar;
  az = az ^ an;
  az = rotate((uint)(az), (uint)(32 - (16)));
  av = av + az;
  ar = ar ^ av;
  ar = rotate((uint)(ar), (uint)(32 - (12)));
  ao = ao + (ak ^ Gn);
  ao = ao + as;
  ba = ba ^ ao;
  ba = rotate((uint)(ba), (uint)(32 - (16)));
  aw = aw + ba;
  as = as ^ aw;
  as = rotate((uint)(as), (uint)(32 - (12)));
  an = an + (x ^ Gf);
  an = an + ar;
  az = az ^ an;
  az = rotate((uint)(az), (uint)(32 - (8)));
  av = av + az;
  ar = ar ^ av;
  ar = rotate((uint)(ar), (uint)(32 - (7)));
  ao = ao + (ai ^ Gp);
  ao = ao + as;
  ba = ba ^ ao;
  ba = rotate((uint)(ba), (uint)(32 - (8)));
  aw = aw + ba;
  as = as ^ aw;
  as = rotate((uint)(as), (uint)(32 - (7)));
  am = am + (v ^ Gm);
  am = am + aq;
  ay = ay ^ am;
  ay = rotate((uint)(ay), (uint)(32 - (8)));
  au = au + ay;
  aq = aq ^ au;
  aq = rotate((uint)(aq), (uint)(32 - (7)));
  al = al + (ad ^ Gl);
  al = al + ap;
  ax = ax ^ al;
  ax = rotate((uint)(ax), (uint)(32 - (8)));
  at = at + ax;
  ap = ap ^ at;
  ap = rotate((uint)(ap), (uint)(32 - (7)));
  al = al + (af ^ Go);
  al = al + aq;
  ba = ba ^ al;
  ba = rotate((uint)(ba), (uint)(32 - (16)));
  av = av + ba;
  aq = aq ^ av;
  aq = rotate((uint)(aq), (uint)(32 - (12)));
  am = am + (y ^ Gg);
  am = am + ar;
  ax = ax ^ am;
  ax = rotate((uint)(ax), (uint)(32 - (16)));
  aw = aw + ax;
  ar = ar ^ aw;
  ar = rotate((uint)(ar), (uint)(32 - (12)));
  an = an + (ac ^ Gb);
  an = an + as;
  ay = ay ^ an;
  ay = rotate((uint)(ay), (uint)(32 - (16)));
  at = at + ay;
  as = as ^ at;
  as = rotate((uint)(as), (uint)(32 - (12)));
  ao = ao + (ae ^ Ge);
  ao = ao + ap;
  az = az ^ ao;
  az = rotate((uint)(az), (uint)(32 - (16)));
  au = au + az;
  ap = ap ^ au;
  ap = rotate((uint)(ap), (uint)(32 - (12)));
  an = an + (w ^ Gh);
  an = an + as;
  ay = ay ^ an;
  ay = rotate((uint)(ay), (uint)(32 - (8)));
  at = at + ay;
  as = as ^ at;
  as = rotate((uint)(as), (uint)(32 - (7)));
  ao = ao + (z ^ Gj);
  ao = ao + ap;
  az = az ^ ao;
  az = rotate((uint)(az), (uint)(32 - (8)));
  au = au + az;
  ap = ap ^ au;
  ap = rotate((uint)(ap), (uint)(32 - (7)));
  am = am + (ab ^ Gd);
  am = am + ar;
  ax = ax ^ am;
  ax = rotate((uint)(ax), (uint)(32 - (8)));
  aw = aw + ax;
  ar = ar ^ aw;
  ar = rotate((uint)(ar), (uint)(32 - (7)));
  al = al + (aj ^ Gk);
  al = al + aq;
  ba = ba ^ al;
  ba = rotate((uint)(ba), (uint)(32 - (8)));
  av = av + ba;
  aq = aq ^ av;
  aq = rotate((uint)(aq), (uint)(32 - (7)));

  al = al + (ac ^ Gj);
  al = al + ap;
  ax = ax ^ al;
  ax = rotate((uint)(ax), (uint)(32 - (16)));
  at = at + ax;
  ap = ap ^ at;
  ap = rotate((uint)(ap), (uint)(32 - (12)));
  am = am + (y ^ Gb);
  am = am + aq;
  ay = ay ^ am;
  ay = rotate((uint)(ay), (uint)(32 - (16)));
  au = au + ay;
  aq = aq ^ au;
  aq = rotate((uint)(aq), (uint)(32 - (12)));
  an = an + (ai ^ Gm);
  an = an + ar;
  az = az ^ an;
  az = rotate((uint)(az), (uint)(32 - (16)));
  av = av + az;
  ar = ar ^ av;
  ar = rotate((uint)(ar), (uint)(32 - (12)));
  ao = ao + (ag ^ Go);
  ao = ao + as;
  ba = ba ^ ao;
  ba = rotate((uint)(ba), (uint)(32 - (16)));
  aw = aw + ba;
  as = as ^ aw;
  as = rotate((uint)(as), (uint)(32 - (12)));
  an = an + (ah ^ Gn);
  an = an + ar;
  az = az ^ an;
  az = rotate((uint)(az), (uint)(32 - (8)));
  av = av + az;
  ar = ar ^ av;
  ar = rotate((uint)(ar), (uint)(32 - (7)));
  ao = ao + (aj ^ Gl);
  ao = ao + as;
  ba = ba ^ ao;
  ba = rotate((uint)(ba), (uint)(32 - (8)));
  aw = aw + ba;
  as = as ^ aw;
  as = rotate((uint)(as), (uint)(32 - (7)));
  am = am + (w ^ Gd);
  am = am + aq;
  ay = ay ^ am;
  ay = rotate((uint)(ay), (uint)(32 - (8)));
  au = au + ay;
  aq = aq ^ au;
  aq = rotate((uint)(aq), (uint)(32 - (7)));
  al = al + (ae ^ Gh);
  al = al + ap;
  ax = ax ^ al;
  ax = rotate((uint)(ax), (uint)(32 - (8)));
  at = at + ax;
  ap = ap ^ at;
  ap = rotate((uint)(ap), (uint)(32 - (7)));
  al = al + (x ^ Gg);
  al = al + aq;
  ba = ba ^ al;
  ba = rotate((uint)(ba), (uint)(32 - (16)));
  av = av + ba;
  aq = aq ^ av;
  aq = rotate((uint)(aq), (uint)(32 - (12)));
  am = am + (aa ^ Gk);
  am = am + ar;
  ax = ax ^ am;
  ax = rotate((uint)(ax), (uint)(32 - (16)));
  aw = aw + ax;
  ar = ar ^ aw;
  ar = rotate((uint)(ar), (uint)(32 - (12)));
  an = an + (z ^ Ga);
  an = an + as;
  ay = ay ^ an;
  ay = rotate((uint)(ay), (uint)(32 - (16)));
  at = at + ay;
  as = as ^ at;
  as = rotate((uint)(as), (uint)(32 - (12)));
  ao = ao + (ak ^ Gi);
  ao = ao + ap;
  az = az ^ ao;
  az = rotate((uint)(az), (uint)(32 - (16)));
  au = au + az;
  ap = ap ^ au;
  ap = rotate((uint)(ap), (uint)(32 - (12)));
  an = an + (v ^ Ge);
  an = an + as;
  ay = ay ^ an;
  ay = rotate((uint)(ay), (uint)(32 - (8)));
  at = at + ay;
  as = as ^ at;
  as = rotate((uint)(as), (uint)(32 - (7)));
  ao = ao + (ad ^ Gp);
  ao = ao + ap;
  az = az ^ ao;
  az = rotate((uint)(az), (uint)(32 - (8)));
  au = au + az;
  ap = ap ^ au;
  ap = rotate((uint)(ap), (uint)(32 - (7)));
  am = am + (af ^ Gf);
  am = am + ar;
  ax = ax ^ am;
  ax = rotate((uint)(ax), (uint)(32 - (8)));
  aw = aw + ax;
  ar = ar ^ aw;
  ar = rotate((uint)(ar), (uint)(32 - (7)));
  al = al + (ab ^ Gc);
  al = al + aq;
  ba = ba ^ al;
  ba = rotate((uint)(ba), (uint)(32 - (8)));
  av = av + ba;
  aq = aq ^ av;
  aq = rotate((uint)(aq), (uint)(32 - (7)));

  al = al + (ae ^ Ga);
  al = al + ap;
  ax = ax ^ al;
  ax = rotate((uint)(ax), (uint)(32 - (16)));
  at = at + ax;
  ap = ap ^ at;
  ap = rotate((uint)(ap), (uint)(32 - (12)));
  am = am + (aa ^ Gh);
  am = am + aq;
  ay = ay ^ am;
  ay = rotate((uint)(ay), (uint)(32 - (16)));
  au = au + ay;
  aq = aq ^ au;
  aq = rotate((uint)(aq), (uint)(32 - (12)));
  an = an + (x ^ Ge);
  an = an + ar;
  az = az ^ an;
  az = rotate((uint)(az), (uint)(32 - (16)));
  av = av + az;
  ar = ar ^ av;
  ar = rotate((uint)(ar), (uint)(32 - (12)));
  ao = ao + (af ^ Gp);
  ao = ao + as;
  ba = ba ^ ao;
  ba = rotate((uint)(ba), (uint)(32 - (16)));
  aw = aw + ba;
  as = as ^ aw;
  as = rotate((uint)(as), (uint)(32 - (12)));
  an = an + (z ^ Gc);
  an = an + ar;
  az = az ^ an;
  az = rotate((uint)(az), (uint)(32 - (8)));
  av = av + az;
  ar = ar ^ av;
  ar = rotate((uint)(ar), (uint)(32 - (7)));
  ao = ao + (ak ^ Gk);
  ao = ao + as;
  ba = ba ^ ao;
  ba = rotate((uint)(ba), (uint)(32 - (8)));
  aw = aw + ba;
  as = as ^ aw;
  as = rotate((uint)(as), (uint)(32 - (7)));
  am = am + (ac ^ Gf);
  am = am + aq;
  ay = ay ^ am;
  ay = rotate((uint)(ay), (uint)(32 - (8)));
  au = au + ay;
  aq = aq ^ au;
  aq = rotate((uint)(aq), (uint)(32 - (7)));
  al = al + (v ^ Gj);
  al = al + ap;
  ax = ax ^ al;
  ax = rotate((uint)(ax), (uint)(32 - (8)));
  at = at + ax;
  ap = ap ^ at;
  ap = rotate((uint)(ap), (uint)(32 - (7)));
  al = al + (aj ^ Gb);
  al = al + aq;
  ba = ba ^ al;
  ba = rotate((uint)(ba), (uint)(32 - (16)));
  av = av + ba;
  aq = aq ^ av;
  aq = rotate((uint)(aq), (uint)(32 - (12)));
  am = am + (ag ^ Gm);
  am = am + ar;
  ax = ax ^ am;
  ax = rotate((uint)(ax), (uint)(32 - (16)));
  aw = aw + ax;
  ar = ar ^ aw;
  ar = rotate((uint)(ar), (uint)(32 - (12)));
  an = an + (ab ^ Gi);
  an = an + as;
  ay = ay ^ an;
  ay = rotate((uint)(ay), (uint)(32 - (16)));
  at = at + ay;
  as = as ^ at;
  as = rotate((uint)(as), (uint)(32 - (12)));
  ao = ao + (y ^ Gn);
  ao = ao + ap;
  az = az ^ ao;
  az = rotate((uint)(az), (uint)(32 - (16)));
  au = au + az;
  ap = ap ^ au;
  ap = rotate((uint)(ap), (uint)(32 - (12)));
  an = an + (ad ^ Gg);
  an = an + as;
  ay = ay ^ an;
  ay = rotate((uint)(ay), (uint)(32 - (8)));
  at = at + ay;
  as = as ^ at;
  as = rotate((uint)(as), (uint)(32 - (7)));
  ao = ao + (ai ^ Gd);
  ao = ao + ap;
  az = az ^ ao;
  az = rotate((uint)(az), (uint)(32 - (8)));
  au = au + az;
  ap = ap ^ au;
  ap = rotate((uint)(ap), (uint)(32 - (7)));
  am = am + (ah ^ Gl);
  am = am + ar;
  ax = ax ^ am;
  ax = rotate((uint)(ax), (uint)(32 - (8)));
  aw = aw + ax;
  ar = ar ^ aw;
  ar = rotate((uint)(ar), (uint)(32 - (7)));
  al = al + (w ^ Go);
  al = al + aq;
  ba = ba ^ al;
  ba = rotate((uint)(ba), (uint)(32 - (8)));
  av = av + ba;
  aq = aq ^ av;
  aq = rotate((uint)(aq), (uint)(32 - (7)));

  al = al + (x ^ Gm);
  al = al + ap;
  ax = ax ^ al;
  ax = rotate((uint)(ax), (uint)(32 - (16)));
  at = at + ax;
  ap = ap ^ at;
  ap = rotate((uint)(ap), (uint)(32 - (12)));
  am = am + (ab ^ Gk);
  am = am + aq;
  ay = ay ^ am;
  ay = rotate((uint)(ay), (uint)(32 - (16)));
  au = au + ay;
  aq = aq ^ au;
  aq = rotate((uint)(aq), (uint)(32 - (12)));
  an = an + (v ^ Gl);
  an = an + ar;
  az = az ^ an;
  az = rotate((uint)(az), (uint)(32 - (16)));
  av = av + az;
  ar = ar ^ av;
  ar = rotate((uint)(ar), (uint)(32 - (12)));
  ao = ao + (ad ^ Gd);
  ao = ao + as;
  ba = ba ^ ao;
  ba = rotate((uint)(ba), (uint)(32 - (16)));
  aw = aw + ba;
  as = as ^ aw;
  as = rotate((uint)(as), (uint)(32 - (12)));
  an = an + (ag ^ Ga);
  an = an + ar;
  az = az ^ an;
  az = rotate((uint)(az), (uint)(32 - (8)));
  av = av + az;
  ar = ar ^ av;
  ar = rotate((uint)(ar), (uint)(32 - (7)));
  ao = ao + (y ^ Gi);
  ao = ao + as;
  ba = ba ^ ao;
  ba = rotate((uint)(ba), (uint)(32 - (8)));
  aw = aw + ba;
  as = as ^ aw;
  as = rotate((uint)(as), (uint)(32 - (7)));
  am = am + (af ^ Gg);
  am = am + aq;
  ay = ay ^ am;
  ay = rotate((uint)(ay), (uint)(32 - (8)));
  au = au + ay;
  aq = aq ^ au;
  aq = rotate((uint)(aq), (uint)(32 - (7)));
  al = al + (ah ^ Gc);
  al = al + ap;
  ax = ax ^ al;
  ax = rotate((uint)(ax), (uint)(32 - (8)));
  at = at + ax;
  ap = ap ^ at;
  ap = rotate((uint)(ap), (uint)(32 - (7)));
  al = al + (z ^ Gn);
  al = al + aq;
  ba = ba ^ al;
  ba = rotate((uint)(ba), (uint)(32 - (16)));
  av = av + ba;
  aq = aq ^ av;
  aq = rotate((uint)(aq), (uint)(32 - (12)));
  am = am + (ac ^ Gf);
  am = am + ar;
  ax = ax ^ am;
  ax = rotate((uint)(ax), (uint)(32 - (16)));
  aw = aw + ax;
  ar = ar ^ aw;
  ar = rotate((uint)(ar), (uint)(32 - (12)));
  an = an + (ak ^ Go);
  an = an + as;
  ay = ay ^ an;
  ay = rotate((uint)(ay), (uint)(32 - (16)));
  at = at + ay;
  as = as ^ at;
  as = rotate((uint)(as), (uint)(32 - (12)));
  ao = ao + (w ^ Gj);
  ao = ao + ap;
  az = az ^ ao;
  az = rotate((uint)(az), (uint)(32 - (16)));
  au = au + az;
  ap = ap ^ au;
  ap = rotate((uint)(ap), (uint)(32 - (12)));
  an = an + (aj ^ Gp);
  an = an + as;
  ay = ay ^ an;
  ay = rotate((uint)(ay), (uint)(32 - (8)));
  at = at + ay;
  as = as ^ at;
  as = rotate((uint)(as), (uint)(32 - (7)));
  ao = ao + (ae ^ Gb);
  ao = ao + ap;
  az = az ^ ao;
  az = rotate((uint)(az), (uint)(32 - (8)));
  au = au + az;
  ap = ap ^ au;
  ap = rotate((uint)(ap), (uint)(32 - (7)));
  am = am + (aa ^ Gh);
  am = am + ar;
  ax = ax ^ am;
  ax = rotate((uint)(ax), (uint)(32 - (8)));
  aw = aw + ax;
  ar = ar ^ aw;
  ar = rotate((uint)(ar), (uint)(32 - (7)));
  al = al + (ai ^ Ge);
  al = al + aq;
  ba = ba ^ al;
  ba = rotate((uint)(ba), (uint)(32 - (8)));
  av = av + ba;
  aq = aq ^ av;
  aq = rotate((uint)(aq), (uint)(32 - (7)));

  al = al + (ah ^ Gf);
  al = al + ap;
  ax = ax ^ al;
  ax = rotate((uint)(ax), (uint)(32 - (16)));
  at = at + ax;
  ap = ap ^ at;
  ap = rotate((uint)(ap), (uint)(32 - (12)));
  am = am + (w ^ Gp);
  am = am + aq;
  ay = ay ^ am;
  ay = rotate((uint)(ay), (uint)(32 - (16)));
  au = au + ay;
  aq = aq ^ au;
  aq = rotate((uint)(aq), (uint)(32 - (12)));
  an = an + (aj ^ Gn);
  an = an + ar;
  az = az ^ an;
  az = rotate((uint)(az), (uint)(32 - (16)));
  av = av + az;
  ar = ar ^ av;
  ar = rotate((uint)(ar), (uint)(32 - (12)));
  ao = ao + (z ^ Gk);
  ao = ao + as;
  ba = ba ^ ao;
  ba = rotate((uint)(ba), (uint)(32 - (16)));
  aw = aw + ba;
  as = as ^ aw;
  as = rotate((uint)(as), (uint)(32 - (12)));
  an = an + (ai ^ Go);
  an = an + ar;
  az = az ^ an;
  az = rotate((uint)(az), (uint)(32 - (8)));
  av = av + az;
  ar = ar ^ av;
  ar = rotate((uint)(ar), (uint)(32 - (7)));
  ao = ao + (af ^ Ge);
  ao = ao + as;
  ba = ba ^ ao;
  ba = rotate((uint)(ba), (uint)(32 - (8)));
  aw = aw + ba;
  as = as ^ aw;
  as = rotate((uint)(as), (uint)(32 - (7)));
  am = am + (ak ^ Gb);
  am = am + aq;
  ay = ay ^ am;
  ay = rotate((uint)(ay), (uint)(32 - (8)));
  au = au + ay;
  aq = aq ^ au;
  aq = rotate((uint)(aq), (uint)(32 - (7)));
  al = al + (aa ^ Gm);
  al = al + ap;
  ax = ax ^ al;
  ax = rotate((uint)(ax), (uint)(32 - (8)));
  at = at + ax;
  ap = ap ^ at;
  ap = rotate((uint)(ap), (uint)(32 - (7)));
  al = al + (v ^ Gh);
  al = al + aq;
  ba = ba ^ al;
  ba = rotate((uint)(ba), (uint)(32 - (16)));
  av = av + ba;
  aq = aq ^ av;
  aq = rotate((uint)(aq), (uint)(32 - (12)));
  am = am + (ab ^ Gd);
  am = am + ar;
  ax = ax ^ am;
  ax = rotate((uint)(ax), (uint)(32 - (16)));
  aw = aw + ax;
  ar = ar ^ aw;
  ar = rotate((uint)(ar), (uint)(32 - (12)));
  an = an + (ae ^ Gc);
  an = an + as;
  ay = ay ^ an;
  ay = rotate((uint)(ay), (uint)(32 - (16)));
  at = at + ay;
  as = as ^ at;
  as = rotate((uint)(as), (uint)(32 - (12)));
  ao = ao + (ad ^ Gl);
  ao = ao + ap;
  az = az ^ ao;
  az = rotate((uint)(az), (uint)(32 - (16)));
  au = au + az;
  ap = ap ^ au;
  ap = rotate((uint)(ap), (uint)(32 - (12)));
  an = an + (x ^ Gj);
  an = an + as;
  ay = ay ^ an;
  ay = rotate((uint)(ay), (uint)(32 - (8)));
  at = at + ay;
  as = as ^ at;
  as = rotate((uint)(as), (uint)(32 - (7)));
  ao = ao + (ag ^ Gi);
  ao = ao + ap;
  az = az ^ ao;
  az = rotate((uint)(az), (uint)(32 - (8)));
  au = au + az;
  ap = ap ^ au;
  ap = rotate((uint)(ap), (uint)(32 - (7)));
  am = am + (y ^ Gg);
  am = am + ar;
  ax = ax ^ am;
  ax = rotate((uint)(ax), (uint)(32 - (8)));
  aw = aw + ax;
  ar = ar ^ aw;
  ar = rotate((uint)(ar), (uint)(32 - (7)));
  al = al + (ac ^ Ga);
  al = al + aq;
  ba = ba ^ al;
  ba = rotate((uint)(ba), (uint)(32 - (8)));
  av = av + ba;
  aq = aq ^ av;
  aq = rotate((uint)(aq), (uint)(32 - (7)));

  al = al + (ai ^ Gl);
  al = al + ap;
  ax = ax ^ al;
  ax = rotate((uint)(ax), (uint)(32 - (16)));
  at = at + ax;
  ap = ap ^ at;
  ap = rotate((uint)(ap), (uint)(32 - (12)));
  am = am + (ac ^ Go);
  am = am + aq;
  ay = ay ^ am;
  ay = rotate((uint)(ay), (uint)(32 - (16)));
  au = au + ay;
  aq = aq ^ au;
  aq = rotate((uint)(aq), (uint)(32 - (12)));
  an = an + (ah ^ Gb);
  an = an + ar;
  az = az ^ an;
  az = rotate((uint)(az), (uint)(32 - (16)));
  av = av + az;
  ar = ar ^ av;
  ar = rotate((uint)(ar), (uint)(32 - (12)));
  ao = ao + (y ^ Gj);
  ao = ao + as;
  ba = ba ^ ao;
  ba = rotate((uint)(ba), (uint)(32 - (16)));
  aw = aw + ba;
  as = as ^ aw;
  as = rotate((uint)(as), (uint)(32 - (12)));
  an = an + (w ^ Gm);
  an = an + ar;
  az = az ^ an;
  az = rotate((uint)(az), (uint)(32 - (8)));
  av = av + az;
  ar = ar ^ av;
  ar = rotate((uint)(ar), (uint)(32 - (7)));
  ao = ao + (ae ^ Gd);
  ao = ao + as;
  ba = ba ^ ao;
  ba = rotate((uint)(ba), (uint)(32 - (8)));
  aw = aw + ba;
  as = as ^ aw;
  as = rotate((uint)(as), (uint)(32 - (7)));
  am = am + (aj ^ Gh);
  am = am + aq;
  ay = ay ^ am;
  ay = rotate((uint)(ay), (uint)(32 - (8)));
  au = au + ay;
  aq = aq ^ au;
  aq = rotate((uint)(aq), (uint)(32 - (7)));
  al = al + (ag ^ Gn);
  al = al + ap;
  ax = ax ^ al;
  ax = rotate((uint)(ax), (uint)(32 - (8)));
  at = at + ax;
  ap = ap ^ at;
  ap = rotate((uint)(ap), (uint)(32 - (7)));
  al = al + (aa ^ Ga);
  al = al + aq;
  ba = ba ^ al;
  ba = rotate((uint)(ba), (uint)(32 - (16)));
  av = av + ba;
  aq = aq ^ av;
  aq = rotate((uint)(aq), (uint)(32 - (12)));
  am = am + (ak ^ Ge);
  am = am + ar;
  ax = ax ^ am;
  ax = rotate((uint)(ax), (uint)(32 - (16)));
  aw = aw + ax;
  ar = ar ^ aw;
  ar = rotate((uint)(ar), (uint)(32 - (12)));
  an = an + (ad ^ Gg);
  an = an + as;
  ay = ay ^ an;
  ay = rotate((uint)(ay), (uint)(32 - (16)));
  at = at + ay;
  as = as ^ at;
  as = rotate((uint)(as), (uint)(32 - (12)));
  ao = ao + (x ^ Gk);
  ao = ao + ap;
  az = az ^ ao;
  az = rotate((uint)(az), (uint)(32 - (16)));
  au = au + az;
  ap = ap ^ au;
  ap = rotate((uint)(ap), (uint)(32 - (12)));
  an = an + (ab ^ Gi);
  an = an + as;
  ay = ay ^ an;
  ay = rotate((uint)(ay), (uint)(32 - (8)));
  at = at + ay;
  as = as ^ at;
  as = rotate((uint)(as), (uint)(32 - (7)));
  ao = ao + (af ^ Gc);
  ao = ao + ap;
  az = az ^ ao;
  az = rotate((uint)(az), (uint)(32 - (8)));
  au = au + az;
  ap = ap ^ au;
  ap = rotate((uint)(ap), (uint)(32 - (7)));
  am = am + (z ^ Gp);
  am = am + ar;
  ax = ax ^ am;
  ax = rotate((uint)(ax), (uint)(32 - (8)));
  aw = aw + ax;
  ar = ar ^ aw;
  ar = rotate((uint)(ar), (uint)(32 - (7)));
  al = al + (v ^ Gf);
  al = al + aq;
  ba = ba ^ al;
  ba = rotate((uint)(ba), (uint)(32 - (8)));
  av = av + ba;
  aq = aq ^ av;
  aq = rotate((uint)(aq), (uint)(32 - (7)));

  al = al + (ab ^ Gp);
  al = al + ap;
  ax = ax ^ al;
  ax = rotate((uint)(ax), (uint)(32 - (16)));
  at = at + ax;
  ap = ap ^ at;
  ap = rotate((uint)(ap), (uint)(32 - (12)));
  am = am + (aj ^ Gj);
  am = am + aq;
  ay = ay ^ am;
  ay = rotate((uint)(ay), (uint)(32 - (16)));
  au = au + ay;
  aq = aq ^ au;
  aq = rotate((uint)(aq), (uint)(32 - (12)));
  an = an + (ag ^ Gd);
  an = an + ar;
  az = az ^ an;
  az = rotate((uint)(az), (uint)(32 - (16)));
  av = av + az;
  ar = ar ^ av;
  ar = rotate((uint)(ar), (uint)(32 - (12)));
  ao = ao + (v ^ Gi);
  ao = ao + as;
  ba = ba ^ ao;
  ba = rotate((uint)(ba), (uint)(32 - (16)));
  aw = aw + ba;
  as = as ^ aw;
  as = rotate((uint)(as), (uint)(32 - (12)));
  an = an + (y ^ Gl);
  an = an + ar;
  az = az ^ an;
  az = rotate((uint)(az), (uint)(32 - (8)));
  av = av + az;
  ar = ar ^ av;
  ar = rotate((uint)(ar), (uint)(32 - (7)));
  ao = ao + (ad ^ Ga);
  ao = ao + as;
  ba = ba ^ ao;
  ba = rotate((uint)(ba), (uint)(32 - (8)));
  aw = aw + ba;
  as = as ^ aw;
  as = rotate((uint)(as), (uint)(32 - (7)));
  am = am + (ae ^ Go);
  am = am + aq;
  ay = ay ^ am;
  ay = rotate((uint)(ay), (uint)(32 - (8)));
  au = au + ay;
  aq = aq ^ au;
  aq = rotate((uint)(aq), (uint)(32 - (7)));
  al = al + (ak ^ Gg);
  al = al + ap;
  ax = ax ^ al;
  ax = rotate((uint)(ax), (uint)(32 - (8)));
  at = at + ax;
  ap = ap ^ at;
  ap = rotate((uint)(ap), (uint)(32 - (7)));
  al = al + (ah ^ Gc);
  al = al + aq;
  ba = ba ^ al;
  ba = rotate((uint)(ba), (uint)(32 - (16)));
  av = av + ba;
  aq = aq ^ av;
  aq = rotate((uint)(aq), (uint)(32 - (12)));
  am = am + (ai ^ Gh);
  am = am + ar;
  ax = ax ^ am;
  ax = rotate((uint)(ax), (uint)(32 - (16)));
  aw = aw + ax;
  ar = ar ^ aw;
  ar = rotate((uint)(ar), (uint)(32 - (12)));
  an = an + (w ^ Ge);
  an = an + as;
  ay = ay ^ an;
  ay = rotate((uint)(ay), (uint)(32 - (16)));
  at = at + ay;
  as = as ^ at;
  as = rotate((uint)(as), (uint)(32 - (12)));
  ao = ao + (af ^ Gf);
  ao = ao + ap;
  az = az ^ ao;
  az = rotate((uint)(az), (uint)(32 - (16)));
  au = au + az;
  ap = ap ^ au;
  ap = rotate((uint)(ap), (uint)(32 - (12)));
  an = an + (z ^ Gb);
  an = an + as;
  ay = ay ^ an;
  ay = rotate((uint)(ay), (uint)(32 - (8)));
  at = at + ay;
  as = as ^ at;
  as = rotate((uint)(as), (uint)(32 - (7)));
  ao = ao + (aa ^ Gk);
  ao = ao + ap;
  az = az ^ ao;
  az = rotate((uint)(az), (uint)(32 - (8)));
  au = au + az;
  ap = ap ^ au;
  ap = rotate((uint)(ap), (uint)(32 - (7)));
  am = am + (ac ^ Gn);
  am = am + ar;
  ax = ax ^ am;
  ax = rotate((uint)(ax), (uint)(32 - (8)));
  aw = aw + ax;
  ar = ar ^ aw;
  ar = rotate((uint)(ar), (uint)(32 - (7)));
  al = al + (x ^ Gm);
  al = al + aq;
  ba = ba ^ al;
  ba = rotate((uint)(ba), (uint)(32 - (8)));
  av = av + ba;
  aq = aq ^ av;
  aq = rotate((uint)(aq), (uint)(32 - (7)));

  al = al + (af ^ Gc);
  al = al + ap;
  ax = ax ^ al;
  ax = rotate((uint)(ax), (uint)(32 - (16)));
  at = at + ax;
  ap = ap ^ at;
  ap = rotate((uint)(ap), (uint)(32 - (12)));
  am = am + (ad ^ Ge);
  am = am + aq;
  ay = ay ^ am;
  ay = rotate((uint)(ay), (uint)(32 - (16)));
  au = au + ay;
  aq = aq ^ au;
  aq = rotate((uint)(aq), (uint)(32 - (12)));
  an = an + (ac ^ Gg);
  an = an + ar;
  az = az ^ an;
  az = rotate((uint)(az), (uint)(32 - (16)));
  av = av + az;
  ar = ar ^ av;
  ar = rotate((uint)(ar), (uint)(32 - (12)));
  ao = ao + (w ^ Gf);
  ao = ao + as;
  ba = ba ^ ao;
  ba = rotate((uint)(ba), (uint)(32 - (16)));
  aw = aw + ba;
  as = as ^ aw;
  as = rotate((uint)(as), (uint)(32 - (12)));
  an = an + (ab ^ Gh);
  an = an + ar;
  az = az ^ an;
  az = rotate((uint)(az), (uint)(32 - (8)));
  av = av + az;
  ar = ar ^ av;
  ar = rotate((uint)(ar), (uint)(32 - (7)));
  ao = ao + (aa ^ Gb);
  ao = ao + as;
  ba = ba ^ ao;
  ba = rotate((uint)(ba), (uint)(32 - (8)));
  aw = aw + ba;
  as = as ^ aw;
  as = rotate((uint)(as), (uint)(32 - (7)));
  am = am + (z ^ Gi);
  am = am + aq;
  ay = ay ^ am;
  ay = rotate((uint)(ay), (uint)(32 - (8)));
  au = au + ay;
  aq = aq ^ au;
  aq = rotate((uint)(aq), (uint)(32 - (7)));
  al = al + (x ^ Gk);
  al = al + ap;
  ax = ax ^ al;
  ax = rotate((uint)(ax), (uint)(32 - (8)));
  at = at + ax;
  ap = ap ^ at;
  ap = rotate((uint)(ap), (uint)(32 - (7)));
  al = al + (ak ^ Gl);
  al = al + aq;
  ba = ba ^ al;
  ba = rotate((uint)(ba), (uint)(32 - (16)));
  av = av + ba;
  aq = aq ^ av;
  aq = rotate((uint)(aq), (uint)(32 - (12)));
  am = am + (ae ^ Go);
  am = am + ar;
  ax = ax ^ am;
  ax = rotate((uint)(ax), (uint)(32 - (16)));
  aw = aw + ax;
  ar = ar ^ aw;
  ar = rotate((uint)(ar), (uint)(32 - (12)));
  an = an + (y ^ Gm);
  an = an + as;
  ay = ay ^ an;
  ay = rotate((uint)(ay), (uint)(32 - (16)));
  at = at + ay;
  as = as ^ at;
  as = rotate((uint)(as), (uint)(32 - (12)));
  ao = ao + (ai ^ Ga);
  ao = ao + ap;
  az = az ^ ao;
  az = rotate((uint)(az), (uint)(32 - (16)));
  au = au + az;
  ap = ap ^ au;
  ap = rotate((uint)(ap), (uint)(32 - (12)));
  an = an + (ah ^ Gd);
  an = an + as;
  ay = ay ^ an;
  ay = rotate((uint)(ay), (uint)(32 - (8)));
  at = at + ay;
  as = as ^ at;
  as = rotate((uint)(as), (uint)(32 - (7)));
  ao = ao + (v ^ Gn);
  ao = ao + ap;
  az = az ^ ao;
  az = rotate((uint)(az), (uint)(32 - (8)));
  au = au + az;
  ap = ap ^ au;
  ap = rotate((uint)(ap), (uint)(32 - (7)));
  am = am + (aj ^ Gj);
  am = am + ar;
  ax = ax ^ am;
  ax = rotate((uint)(ax), (uint)(32 - (8)));
  aw = aw + ax;
  ar = ar ^ aw;
  ar = rotate((uint)(ar), (uint)(32 - (7)));
  al = al + (ag ^ Gp);
  al = al + aq;
  ba = ba ^ al;
  ba = rotate((uint)(ba), (uint)(32 - (8)));
  av = av + ba;
  aq = aq ^ av;
  aq = rotate((uint)(aq), (uint)(32 - (7)));

  al = al + (v ^ Gb);
  al = al + ap;
  ax = ax ^ al;
  ax = rotate((uint)(ax), (uint)(32 - (16)));
  at = at + ax;
  ap = ap ^ at;
  ap = rotate((uint)(ap), (uint)(32 - (12)));
  am = am + (x ^ Gd);
  am = am + aq;
  ay = ay ^ am;
  ay = rotate((uint)(ay), (uint)(32 - (16)));
  au = au + ay;
  aq = aq ^ au;
  aq = rotate((uint)(aq), (uint)(32 - (12)));
  an = an + (z ^ Gf);
  an = an + ar;
  az = az ^ an;
  az = rotate((uint)(az), (uint)(32 - (16)));
  av = av + az;
  ar = ar ^ av;
  ar = rotate((uint)(ar), (uint)(32 - (12)));
  ao = ao + (ab ^ Gh);
  ao = ao + as;
  ba = ba ^ ao;
  ba = rotate((uint)(ba), (uint)(32 - (16)));
  aw = aw + ba;
  as = as ^ aw;
  as = rotate((uint)(as), (uint)(32 - (12)));
  an = an + (aa ^ Ge);
  an = an + ar;
  az = az ^ an;
  az = rotate((uint)(az), (uint)(32 - (8)));
  av = av + az;
  ar = ar ^ av;
  ar = rotate((uint)(ar), (uint)(32 - (7)));
  ao = ao + (ac ^ Gg);
  ao = ao + as;
  ba = ba ^ ao;
  ba = rotate((uint)(ba), (uint)(32 - (8)));
  aw = aw + ba;
  as = as ^ aw;
  as = rotate((uint)(as), (uint)(32 - (7)));
  am = am + (y ^ Gc);
  am = am + aq;
  ay = ay ^ am;
  ay = rotate((uint)(ay), (uint)(32 - (8)));
  au = au + ay;
  aq = aq ^ au;
  aq = rotate((uint)(aq), (uint)(32 - (7)));
  al = al + (w ^ Ga);
  al = al + ap;
  ax = ax ^ al;
  ax = rotate((uint)(ax), (uint)(32 - (8)));
  at = at + ax;
  ap = ap ^ at;
  ap = rotate((uint)(ap), (uint)(32 - (7)));
  al = al + (ad ^ Gj);
  al = al + aq;
  ba = ba ^ al;
  ba = rotate((uint)(ba), (uint)(32 - (16)));
  av = av + ba;
  aq = aq ^ av;
  aq = rotate((uint)(aq), (uint)(32 - (12)));
  am = am + (af ^ Gl);
  am = am + ar;
  ax = ax ^ am;
  ax = rotate((uint)(ax), (uint)(32 - (16)));
  aw = aw + ax;
  ar = ar ^ aw;
  ar = rotate((uint)(ar), (uint)(32 - (12)));
  an = an + (ah ^ Gn);
  an = an + as;
  ay = ay ^ an;
  ay = rotate((uint)(ay), (uint)(32 - (16)));
  at = at + ay;
  as = as ^ at;
  as = rotate((uint)(as), (uint)(32 - (12)));
  ao = ao + (aj ^ Gp);
  ao = ao + ap;
  az = az ^ ao;
  az = rotate((uint)(az), (uint)(32 - (16)));
  au = au + az;
  ap = ap ^ au;
  ap = rotate((uint)(ap), (uint)(32 - (12)));
  an = an + (ai ^ Gm);
  an = an + as;
  ay = ay ^ an;
  ay = rotate((uint)(ay), (uint)(32 - (8)));
  at = at + ay;
  as = as ^ at;
  as = rotate((uint)(as), (uint)(32 - (7)));
  ao = ao + (ak ^ Go);
  ao = ao + ap;
  az = az ^ ao;
  az = rotate((uint)(az), (uint)(32 - (8)));
  au = au + az;
  ap = ap ^ au;
  ap = rotate((uint)(ap), (uint)(32 - (7)));
  am = am + (ag ^ Gk);
  am = am + ar;
  ax = ax ^ am;
  ax = rotate((uint)(ax), (uint)(32 - (8)));
  aw = aw + ax;
  ar = ar ^ aw;
  ar = rotate((uint)(ar), (uint)(32 - (7)));
  al = al + (ae ^ Gi);
  al = al + aq;
  ba = ba ^ al;
  ba = rotate((uint)(ba), (uint)(32 - (8)));
  av = av + ba;
  aq = aq ^ av;
  aq = rotate((uint)(aq), (uint)(32 - (7)));

  al = al + (aj ^ Gk);
  al = al + ap;
  ax = ax ^ al;
  ax = rotate((uint)(ax), (uint)(32 - (16)));
  at = at + ax;
  ap = ap ^ at;
  ap = rotate((uint)(ap), (uint)(32 - (12)));
  am = am + (z ^ Gi);
  am = am + aq;
  ay = ay ^ am;
  ay = rotate((uint)(ay), (uint)(32 - (16)));
  au = au + ay;
  aq = aq ^ au;
  aq = rotate((uint)(aq), (uint)(32 - (12)));
  an = an + (ae ^ Gp);
  an = an + ar;
  az = az ^ an;
  az = rotate((uint)(az), (uint)(32 - (16)));
  av = av + az;
  ar = ar ^ av;
  ar = rotate((uint)(ar), (uint)(32 - (12)));
  ao = ao + (ai ^ Gg);
  ao = ao + as;
  ba = ba ^ ao;
  ba = rotate((uint)(ba), (uint)(32 - (16)));
  aw = aw + ba;
  as = as ^ aw;
  as = rotate((uint)(as), (uint)(32 - (12)));
  an = an + (ak ^ Gj);
  an = an + ar;
  az = az ^ an;
  az = rotate((uint)(az), (uint)(32 - (8)));
  av = av + az;
  ar = ar ^ av;
  ar = rotate((uint)(ar), (uint)(32 - (7)));
  ao = ao + (ab ^ Gn);
  ao = ao + as;
  ba = ba ^ ao;
  ba = rotate((uint)(ba), (uint)(32 - (8)));
  aw = aw + ba;
  as = as ^ aw;
  as = rotate((uint)(as), (uint)(32 - (7)));
  am = am + (ad ^ Ge);
  am = am + aq;
  ay = ay ^ am;
  ay = rotate((uint)(ay), (uint)(32 - (8)));
  au = au + ay;
  aq = aq ^ au;
  aq = rotate((uint)(aq), (uint)(32 - (7)));
  al = al + (af ^ Go);
  al = al + ap;
  ax = ax ^ al;
  ax = rotate((uint)(ax), (uint)(32 - (8)));
  at = at + ax;
  ap = ap ^ at;
  ap = rotate((uint)(ap), (uint)(32 - (7)));
  al = al + (w ^ Gm);
  al = al + aq;
  ba = ba ^ al;
  ba = rotate((uint)(ba), (uint)(32 - (16)));
  av = av + ba;
  aq = aq ^ av;
  aq = rotate((uint)(aq), (uint)(32 - (12)));
  am = am + (v ^ Gc);
  am = am + ar;
  ax = ax ^ am;
  ax = rotate((uint)(ax), (uint)(32 - (16)));
  aw = aw + ax;
  ar = ar ^ aw;
  ar = rotate((uint)(ar), (uint)(32 - (12)));
  an = an + (ag ^ Gh);
  an = an + as;
  ay = ay ^ an;
  ay = rotate((uint)(ay), (uint)(32 - (16)));
  at = at + ay;
  as = as ^ at;
  as = rotate((uint)(as), (uint)(32 - (12)));
  ao = ao + (aa ^ Gd);
  ao = ao + ap;
  az = az ^ ao;
  az = rotate((uint)(az), (uint)(32 - (16)));
  au = au + az;
  ap = ap ^ au;
  ap = rotate((uint)(ap), (uint)(32 - (12)));
  an = an + (ac ^ Gl);
  an = an + as;
  ay = ay ^ an;
  ay = rotate((uint)(ay), (uint)(32 - (8)));
  at = at + ay;
  as = as ^ at;
  as = rotate((uint)(as), (uint)(32 - (7)));
  ao = ao + (y ^ Gf);
  ao = ao + ap;
  az = az ^ ao;
  az = rotate((uint)(az), (uint)(32 - (8)));
  au = au + az;
  ap = ap ^ au;
  ap = rotate((uint)(ap), (uint)(32 - (7)));
  am = am + (x ^ Ga);
  am = am + ar;
  ax = ax ^ am;
  ax = rotate((uint)(ax), (uint)(32 - (8)));
  aw = aw + ax;
  ar = ar ^ aw;
  ar = rotate((uint)(ar), (uint)(32 - (7)));
  al = al + (ah ^ Gb);
  al = al + aq;
  ba = ba ^ al;
  ba = rotate((uint)(ba), (uint)(32 - (8)));
  av = av + ba;
  aq = aq ^ av;
  aq = rotate((uint)(aq), (uint)(32 - (7)));

  al = al + (ag ^ Gi);
  al = al + ap;
  ax = ax ^ al;
  ax = rotate((uint)(ax), (uint)(32 - (16)));
  at = at + ax;
  ap = ap ^ at;
  ap = rotate((uint)(ap), (uint)(32 - (12)));
  am = am + (ah ^ Ga);
  am = am + aq;
  ay = ay ^ am;
  ay = rotate((uint)(ay), (uint)(32 - (16)));
  au = au + ay;
  aq = aq ^ au;
  aq = rotate((uint)(aq), (uint)(32 - (12)));
  an = an + (aa ^ Gc);
  an = an + ar;
  az = az ^ an;
  az = rotate((uint)(az), (uint)(32 - (16)));
  av = av + az;
  ar = ar ^ av;
  ar = rotate((uint)(ar), (uint)(32 - (12)));
  ao = ao + (ak ^ Gn);
  ao = ao + as;
  ba = ba ^ ao;
  ba = rotate((uint)(ba), (uint)(32 - (16)));
  aw = aw + ba;
  as = as ^ aw;
  as = rotate((uint)(as), (uint)(32 - (12)));
  an = an + (x ^ Gf);
  an = an + ar;
  az = az ^ an;
  az = rotate((uint)(az), (uint)(32 - (8)));
  av = av + az;
  ar = ar ^ av;
  ar = rotate((uint)(ar), (uint)(32 - (7)));
  ao = ao + (ai ^ Gp);
  ao = ao + as;
  ba = ba ^ ao;
  ba = rotate((uint)(ba), (uint)(32 - (8)));
  aw = aw + ba;
  as = as ^ aw;
  as = rotate((uint)(as), (uint)(32 - (7)));
  am = am + (v ^ Gm);
  am = am + aq;
  ay = ay ^ am;
  ay = rotate((uint)(ay), (uint)(32 - (8)));
  au = au + ay;
  aq = aq ^ au;
  aq = rotate((uint)(aq), (uint)(32 - (7)));
  al = al + (ad ^ Gl);
  al = al + ap;
  ax = ax ^ al;
  ax = rotate((uint)(ax), (uint)(32 - (8)));
  at = at + ax;
  ap = ap ^ at;
  ap = rotate((uint)(ap), (uint)(32 - (7)));
  al = al + (af ^ Go);
  al = al + aq;
  ba = ba ^ al;
  ba = rotate((uint)(ba), (uint)(32 - (16)));
  av = av + ba;
  aq = aq ^ av;
  aq = rotate((uint)(aq), (uint)(32 - (12)));
  am = am + (y ^ Gg);
  am = am + ar;
  ax = ax ^ am;
  ax = rotate((uint)(ax), (uint)(32 - (16)));
  aw = aw + ax;
  ar = ar ^ aw;
  ar = rotate((uint)(ar), (uint)(32 - (12)));
  an = an + (ac ^ Gb);
  an = an + as;
  ay = ay ^ an;
  ay = rotate((uint)(ay), (uint)(32 - (16)));
  at = at + ay;
  as = as ^ at;
  as = rotate((uint)(as), (uint)(32 - (12)));
  ao = ao + (ae ^ Ge);
  ao = ao + ap;
  az = az ^ ao;
  az = rotate((uint)(az), (uint)(32 - (16)));
  au = au + az;
  ap = ap ^ au;
  ap = rotate((uint)(ap), (uint)(32 - (12)));
  an = an + (w ^ Gh);
  an = an + as;
  ay = ay ^ an;
  ay = rotate((uint)(ay), (uint)(32 - (8)));
  at = at + ay;
  as = as ^ at;
  as = rotate((uint)(as), (uint)(32 - (7)));
  ao = ao + (z ^ Gj);
  ao = ao + ap;
  az = az ^ ao;
  az = rotate((uint)(az), (uint)(32 - (8)));
  au = au + az;
  ap = ap ^ au;
  ap = rotate((uint)(ap), (uint)(32 - (7)));
  am = am + (ab ^ Gd);
  am = am + ar;
  ax = ax ^ am;
  ax = rotate((uint)(ax), (uint)(32 - (8)));
  aw = aw + ax;
  ar = ar ^ aw;
  ar = rotate((uint)(ar), (uint)(32 - (7)));
  al = al + (aj ^ Gk);
  al = al + aq;
  ba = ba ^ al;
  ba = rotate((uint)(ba), (uint)(32 - (8)));
  av = av + ba;
  aq = aq ^ av;
  aq = rotate((uint)(aq), (uint)(32 - (7)));

  al = al + (ac ^ Gj);
  al = al + ap;
  ax = ax ^ al;
  ax = rotate((uint)(ax), (uint)(32 - (16)));
  at = at + ax;
  ap = ap ^ at;
  ap = rotate((uint)(ap), (uint)(32 - (12)));
  am = am + (y ^ Gb);
  am = am + aq;
  ay = ay ^ am;
  ay = rotate((uint)(ay), (uint)(32 - (16)));
  au = au + ay;
  aq = aq ^ au;
  aq = rotate((uint)(aq), (uint)(32 - (12)));
  an = an + (ai ^ Gm);
  an = an + ar;
  az = az ^ an;
  az = rotate((uint)(az), (uint)(32 - (16)));
  av = av + az;
  ar = ar ^ av;
  ar = rotate((uint)(ar), (uint)(32 - (12)));
  ao = ao + (ag ^ Go);
  ao = ao + as;
  ba = ba ^ ao;
  ba = rotate((uint)(ba), (uint)(32 - (16)));
  aw = aw + ba;
  as = as ^ aw;
  as = rotate((uint)(as), (uint)(32 - (12)));
  an = an + (ah ^ Gn);
  an = an + ar;
  az = az ^ an;
  az = rotate((uint)(az), (uint)(32 - (8)));
  av = av + az;
  ar = ar ^ av;
  ar = rotate((uint)(ar), (uint)(32 - (7)));
  ao = ao + (aj ^ Gl);
  ao = ao + as;
  ba = ba ^ ao;
  ba = rotate((uint)(ba), (uint)(32 - (8)));
  aw = aw + ba;
  as = as ^ aw;
  as = rotate((uint)(as), (uint)(32 - (7)));
  am = am + (w ^ Gd);
  am = am + aq;
  ay = ay ^ am;
  ay = rotate((uint)(ay), (uint)(32 - (8)));
  au = au + ay;
  aq = aq ^ au;
  aq = rotate((uint)(aq), (uint)(32 - (7)));
  al = al + (ae ^ Gh);
  al = al + ap;
  ax = ax ^ al;
  ax = rotate((uint)(ax), (uint)(32 - (8)));
  at = at + ax;
  ap = ap ^ at;
  ap = rotate((uint)(ap), (uint)(32 - (7)));
  al = al + (x ^ Gg);
  al = al + aq;
  ba = ba ^ al;
  ba = rotate((uint)(ba), (uint)(32 - (16)));
  av = av + ba;
  aq = aq ^ av;
  aq = rotate((uint)(aq), (uint)(32 - (12)));
  am = am + (aa ^ Gk);
  am = am + ar;
  ax = ax ^ am;
  ax = rotate((uint)(ax), (uint)(32 - (16)));
  aw = aw + ax;
  ar = ar ^ aw;
  ar = rotate((uint)(ar), (uint)(32 - (12)));
  an = an + (z ^ Ga);
  an = an + as;
  ay = ay ^ an;
  ay = rotate((uint)(ay), (uint)(32 - (16)));
  at = at + ay;
  as = as ^ at;
  as = rotate((uint)(as), (uint)(32 - (12)));
  ao = ao + (ak ^ Gi);
  ao = ao + ap;
  az = az ^ ao;
  az = rotate((uint)(az), (uint)(32 - (16)));
  au = au + az;
  ap = ap ^ au;
  ap = rotate((uint)(ap), (uint)(32 - (12)));
  an = an + (v ^ Ge);
  an = an + as;
  ay = ay ^ an;
  ay = rotate((uint)(ay), (uint)(32 - (8)));
  at = at + ay;
  as = as ^ at;
  as = rotate((uint)(as), (uint)(32 - (7)));
  ao = ao + (ad ^ Gp);
  ao = ao + ap;
  az = az ^ ao;
  az = rotate((uint)(az), (uint)(32 - (8)));
  au = au + az;
  ap = ap ^ au;
  ap = rotate((uint)(ap), (uint)(32 - (7)));
  am = am + (af ^ Gf);
  am = am + ar;
  ax = ax ^ am;
  ax = rotate((uint)(ax), (uint)(32 - (8)));
  aw = aw + ax;
  ar = ar ^ aw;
  ar = rotate((uint)(ar), (uint)(32 - (7)));
  al = al + (ab ^ Gc);
  al = al + aq;
  ba = ba ^ al;
  ba = rotate((uint)(ba), (uint)(32 - (8)));
  av = av + ba;
  aq = aq ^ av;
  aq = rotate((uint)(aq), (uint)(32 - (7)));
  if (bb ^ as ^ ba)
    return;

  a[++a[0]] = bc;
}