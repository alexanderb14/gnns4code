__kernel void A(__global float* a, __global float* b) {
  int c = (get_group_id(0) * get_local_size(0) * 16) + get_local_id(0);
  float d = 0;

  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  ;
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  ;
  ;

  b[get_global_id(0)] = d;
}

__kernel void B(__global float2* a, __global float* b) {
  int c = (get_group_id(0) * get_local_size(0) * 16) + get_local_id(0);
  float2 d = 0;

  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  ;
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  ;
  ;

  b[get_global_id(0)] = (d.S0) + (d.S1);
}

__kernel void C(__global float4* a, __global float* b) {
  int c = (get_group_id(0) * get_local_size(0) * 16) + get_local_id(0);
  float4 d = 0;

  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  ;
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  ;
  ;

  b[get_global_id(0)] = (d.S0) + (d.S1) + (d.S2) + (d.S3);
}

__kernel void D(__global float8* a, __global float* b) {
  int c = (get_group_id(0) * get_local_size(0) * 16) + get_local_id(0);
  float8 d = 0;

  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  ;
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  ;
  ;

  b[get_global_id(0)] = (d.S0) + (d.S1) + (d.S2) + (d.S3) + (d.S4) + (d.S5) + (d.S6) + (d.S7);
}

__kernel void E(__global float16* a, __global float* b) {
  int c = (get_group_id(0) * get_local_size(0) * 16) + get_local_id(0);
  float16 d = 0;

  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  ;
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  d += a[c];
  c += get_local_size(0);
  d += a[c];
  c += get_local_size(0);
  ;
  ;
  ;

  float e = (d.S0) + (d.S1) + (d.S2) + (d.S3) + (d.S4) + (d.S5) + (d.S6) + (d.S7);
  e += (d.S8) + (d.S9) + (d.SA) + (d.SB) + (d.SC) + (d.SD) + (d.SE) + (d.SF);
  b[get_global_id(0)] = e;
}

__kernel void F(__global float* a, __global float* b) {
  int c = get_global_id(0);
  float d = 0;

  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  ;
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  ;
  ;

  b[get_global_id(0)] = d;
}

__kernel void G(__global float2* a, __global float* b) {
  int c = get_global_id(0);
  float2 d = 0;

  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  ;
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  ;
  ;

  b[get_global_id(0)] = (d.S0) + (d.S1);
}

__kernel void H(__global float4* a, __global float* b) {
  int c = get_global_id(0);
  float4 d = 0;

  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  ;
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  ;
  ;

  b[get_global_id(0)] = (d.S0) + (d.S1) + (d.S2) + (d.S3);
}

__kernel void I(__global float8* a, __global float* b) {
  int c = get_global_id(0);
  float8 d = 0;

  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  ;
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  ;
  ;

  b[get_global_id(0)] = (d.S0) + (d.S1) + (d.S2) + (d.S3) + (d.S4) + (d.S5) + (d.S6) + (d.S7);
}

__kernel void J(__global float16* a, __global float* b) {
  int c = get_global_id(0);
  float16 d = 0;

  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  ;
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  d += a[c];
  c += get_global_size(0);
  d += a[c];
  c += get_global_size(0);
  ;
  ;
  ;

  float e = (d.S0) + (d.S1) + (d.S2) + (d.S3) + (d.S4) + (d.S5) + (d.S6) + (d.S7);
  e += (d.S8) + (d.S9) + (d.SA) + (d.SB) + (d.SC) + (d.SD) + (d.SE) + (d.SF);
  b[get_global_id(0)] = e;
}